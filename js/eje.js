// Definimos una función que queremos exportar
export function saludar(nombre) {
    console.log(`¡Hola, ${nombre}!`);
}

// También podemos exportar variables, objetos o cualquier otra cosa que queramos compartir
export const pi = 3.1416;